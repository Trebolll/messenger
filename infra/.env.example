JWT_SECRET=changeme-jwt-secret-key-32chars-min
CONFIRM_SECRET=changeme-confirm-secret-key-32ch
INTERNAL_KEY=internal-dev-key

# media-service / MinIO — must match the MinIO you already run (not necessarily compose)
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=CHANGE_ME
MINIO_BUCKET=messenger
